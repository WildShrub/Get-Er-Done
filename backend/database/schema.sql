CREATE TABLE userSchedules (
    username VARCHAR(50) PRIMARY KEY NOT NULL,
    userSchedule JSON
);

CREATE TABLE userSchedules (
    username VARCHAR(50) PRIMARY KEY NOT NULL,
    userSchedule JSON
);

CREATE TABLE userSchedules (
    username VARCHAR(50) PRIMARY KEY NOT NULL,
    userSchedule JSON
);

CREATE TABLE userSchedules (
    username VARCHAR(50) PRIMARY KEY NOT NULL,
    userSchedule JSON
);

CREATE TABLE userSchedules (
    username VARCHAR(50) PRIMARY KEY NOT NULL,
    userSchedule JSON
);

CREATE TABLE userSchedules (
    username VARCHAR(50) PRIMARY KEY NOT NULL,
    userSchedule JSON
);